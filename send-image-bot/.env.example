# ─────────────────────────────────────────────────────────────
#  MeetStream Labs: Send Image Bot
#  Copy this file to .env and fill it in.
# ─────────────────────────────────────────────────────────────

# Required. MeetStream dashboard -> API Keys.
# https://app.meetstream.ai
MEETSTREAM_API_KEY=your_meetstream_api_key_here

# ─────────────────────────────────────────────────────────────
#  Video-frame mode only (--mode video-frame)
#
#  This mode needs a public wss:// endpoint, because the bot dials OUT to your
#  control socket. Chat mode needs none of this.
#  Pick ONE of the two:
# ─────────────────────────────────────────────────────────────

# Option A: you already have a public HTTPS endpoint pointing at this process.
# PUBLIC_URL=https://control.example.com

# Option B: let the template open an ngrok tunnel for you (free account is fine).
# https://dashboard.ngrok.com/get-started/your-authtoken
# NGROK_AUTHTOKEN=your_ngrok_authtoken_here

# ─────────────────────────────────────────────────────────────
#  Optional
# ─────────────────────────────────────────────────────────────

# Local port for the control server in video-frame mode (default 3000)
# PORT=3000

# Default display name when this CLI creates a bot. Override with --bot-name.
# BOT_NAME=MeetStream Labs Image Bot
