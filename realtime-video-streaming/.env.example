# ─────────────────────────────────────────────────────────────
#  MeetStream Labs: Real-Time Video Streaming
#  Copy this file to .env and fill it in.
# ─────────────────────────────────────────────────────────────

# Required. MeetStream dashboard -> API Keys.
# https://app.meetstream.ai
MEETSTREAM_API_KEY=your_meetstream_api_key_here

# Required. The meeting the bot should join.
# Live video works on Google Meet and Microsoft Teams. It is NOT available on Zoom.
MEETING_LINK=https://meet.google.com/xxx-xxxx-xxx

# ─────────────────────────────────────────────────────────────
#  Public URL: pick ONE of the two
# ─────────────────────────────────────────────────────────────

# Option A: you already have a public HTTPS endpoint pointing at this process.
# PUBLIC_URL=https://video.example.com

# Option B: let the template open an ngrok tunnel for you (free account is fine).
# https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=your_ngrok_authtoken_here

# ─────────────────────────────────────────────────────────────
#  Optional
# ─────────────────────────────────────────────────────────────

# Local port for the HTTP + WebSocket server (default 3000)
# PORT=3000

# Display name of the bot in the participant list
# BOT_NAME=MeetStream Labs Video Bot

# Where the fMP4 files are written (default ./output)
# OUTPUT_DIR=./output

# Also produce a downloadable post-call recording (GET /bots/{id}/get_video).
# This is separate from live streaming. Default false.
# VIDEO_RECORDING=false

# Max simultaneous consumers allowed on the ws://localhost:PORT/stream relay
# MAX_RELAY_CLIENTS=5

# Used by consumer-example.js if the relay is not on localhost
# RELAY_URL=ws://localhost:3000/stream
