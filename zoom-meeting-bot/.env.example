# ─────────────────────────────────────────────
#  MeetStream - Zoom meeting bot
# ─────────────────────────────────────────────

# Required. Your MeetStream API key (https://app.meetstream.ai).
# Sent as `Authorization: Token <key>` - the literal word "Token", not "Bearer".
MEETSTREAM_API_KEY=

# Optional. Override the API base URL (defaults to https://api.meetstream.ai/api/v1).
# MEETSTREAM_BASE_URL=https://api.meetstream.ai/api/v1

# Required. The full Zoom invite link. If the meeting is password protected,
# keep the ?pwd= component - the bot needs it.
MEETING_LINK=

# Display name shown in the meeting.
# BOT_NAME=MeetStream Notetaker

# Video is OFF by default: this bot records audio only unless you ask for video.
# `video_required: false` is sent explicitly, because the REST API treats an
# omitted `video_required` as true.
# VIDEO_REQUIRED=false

# Only read when VIDEO_REQUIRED is true. "speaker_view" or "grid_view". The API
# default is grid_view, so speaker view is sent explicitly. Use grid_view only
# when you want the composited mosaic of everyone.
# VIDEO_LAYOUT=speaker_view

# ── Webhook receiver ───────────────────────────────────────────────────────────

# PORT=3000
# WEBHOOK_PATH=/webhook

# Required for `node index.js`. The public https origin that reaches this server
# (ngrok, Cloudflare tunnel, deployed host). callback_url = this + WEBHOOK_PATH.
PUBLIC_WEBHOOK_URL=

# Optional. Per-bot callback_url deliveries are NOT signed - set this only when
# receiving on a dashboard workspace webhook endpoint.
# WEBHOOK_SECRET=

# Optional. Alerts are POSTed here as JSON in addition to being logged.
# NOTIFY_WEBHOOK_URL=https://your-app.example.com/alerts

# ── Zoom recording permission ──────────────────────────────────────────────────

# Zoom ONLY. How long the bot waits for the host to answer the recording
# permission prompt before giving up and leaving cleanly.
# Range 60-300 seconds, default 60. Out of range returns HTTP 400.
# 120-180 is usually the cheapest fix when hosts are slow to answer.
# Ignored on Google Meet and Microsoft Teams.
# RECORDING_PERMISSION_DENIED_TIMEOUT=60

# ── Other automatic_leave timeouts (all optional; API defaults shown) ──────────

# How long to wait in the Zoom waiting room. Zoom range: 60-1200 (default 600).
# WAITING_ROOM_TIMEOUT=600

# Leave once the participant count hits zero. Range 60-1800 (default 300).
# EVERYONE_LEFT_TIMEOUT=300

# Hard cap on total recording time. Range 600-18000 (default 14400 = 4 hours).
# Values below 600 return HTTP 400.
# IN_CALL_RECORDING_TIMEOUT=14400

# Local cap: how long `node index.js` waits for bot.done before giving up and
# exiting 1 with a message. Default 330 minutes (20 min waiting room + 4 h
# recording cap + processing headroom). Range 1-1440.
# MAX_SESSION_MINUTES=330

# ── Transcription (optional) ───────────────────────────────────────────────────

# One of: deepgram | assemblyai | sarvam | jigsawstack | meetstream
# meeting_captions is NOT available on Zoom. Unset means no transcript.
# TRANSCRIPT_PROVIDER=deepgram
# TRANSCRIPT_LANGUAGE=en

# Delete stored media after this many hours. Unset means the API default of 720 (30 days).
# RETENTION_HOURS=720

# ── Authenticated Zoom joins (optional) ────────────────────────────────────────
# Set at most ONE. Each is an https URL on your own server that returns a fresh
# Zoom token when the bot calls it at join time. Leave meeting_number off the
# OBF URL; MeetStream appends it. See the zoom-authenticated-joins template and
# https://docs.meetstream.ai/guides/app-integrations/zoom-authenticated-bots
# ZOOM_ZAK_URL=https://api.yourapp.com/zoom/zak?user_id=alice&auth=your_secret
# ZOOM_OBF_URL=https://api.yourapp.com/zoom/obf?user_id=alice&auth=your_secret
