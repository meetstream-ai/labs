# ---------------------------------------------
#  quickstart-first-bot
# ---------------------------------------------

# Required. Your MeetStream API key from https://app.meetstream.ai
MEETSTREAM_API_KEY=your_meetstream_api_key_here

# Required. The meeting the bot should join.
# Works with Zoom, Google Meet and Microsoft Teams links.
MEETING_LINK=https://meet.google.com/xxx-xxxx-xxx

# Optional. Display name the bot uses inside the meeting.
BOT_NAME=Quickstart Bot

# Optional. "true" records video as well as audio. Default is audio only.
# Video is OFF by default: this bot records audio only unless you ask for video.
# `video_required: false` is sent explicitly, because the REST API treats an
# omitted `video_required` as true.
VIDEO_REQUIRED=false

# Only read when VIDEO_REQUIRED is true. "speaker_view" or "grid_view". The API
# default is grid_view, so speaker view is sent explicitly. Use grid_view only
# when you want the composited mosaic of everyone.
# VIDEO_LAYOUT=speaker_view

# Optional. Only change this if you are pointing at a non-production API.
# MEETSTREAM_API_BASE_URL=https://api.meetstream.ai/api/v1
